// app/map.tsx 
// Create this file at: app/map.tsx (NOT inside the (tabs) folder)
import { MapScreen } from '../src/screens/MapScreen/MapScreen';

// Simple default export - Expo Router handles the Stack.Screen automatically
export default function Map() {
  return <MapScreen />;
}